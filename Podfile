# シャープを書くとコメントになる

# ↓最低のiOSバージョン

platform :ios, '13.0'

# ↓swiftのLibraryを使うよ!

use_frameworks!

# target target名do

target 'SampleSource2020' do
    # オンライン上の画像を簡単に読み込む
    pod 'Kingfisher'

    # APIを簡単に扱う
    pod 'Alamofire'
end

